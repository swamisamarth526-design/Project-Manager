{
  "name": "taskflow-mern",
  "version": "1.0.0",
  "packageManager": "npm@10.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "npm install --omit=dev --prefix backend && npm install --include=dev --prefix frontend",
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend",
    "start": "npm run bootstrap && npm run build && npm run start --prefix backend"
  }
}
