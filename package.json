{
  "name": "taskflow-mern",
  "version": "1.0.0",
  "packageManager": "npm@10.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run bootstrap && npm run build && npm run start --prefix backend"
  }
}
